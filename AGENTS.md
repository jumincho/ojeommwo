# ojeommwo-v2 작업 규칙 — v2.6

- 기준 소스와 DB는 pororo 호스트의 `ojeommwo` 컨테이너 `/root/ojeommwo-v2`다. SSH 별칭은 pororo-docker다. 실제 운영에 반영할 프런트 수정·설치·빌드는 서버에서 한다. GitHub 분기의 외부 모델 프런트 작업은 검토할 제안이며, 사용자 지시 전에는 서버·로컬 비상 사본에 배포하지 않는다.
- 관측소는 `observatory/` 하위 프로젝트다. 별도 형제 프로젝트나 임시 공개 터널을 만들지 않는다. 기존 Sites 주소·소유권·공개 범위를 유지한다.
- 로컬 Windows 사본은 비상용이며 평상시 OFF다. 릴리즈 후 소스·관측소 정적 빌드·일관된 7개 운영 저장소를 동기화하고 소스 seal을 검증한다. 자동 offsite 백업은 추가하지 않는다.
- lunch 채널 C0123456789에 실제 테스트 메시지를 절대로 보내지 않는다. 실발송 테스트는 운영자 DM D0123456789만 허용한다. 우선 dry-run과 읽기 전용 가입·권한 검사를 수행한다.
- `/bap`과 함께 먹은 인원 수 입력은 제거된 기능이다. 복원하지 않는다.
- 운영 모델은 gpt-6-luna / xhigh다. 검색·입력 의미 정규화·분류 재심사에 사용한다. 정렬·선호 계산·DB 트랜잭션은 검증 가능한 코드로 수행한다. 새 모델·프롬프트의 첫 요청에서 prompt-cache miss는 정상이다.
- 19개 식사 카테고리만 사용한다. 단독 간식·음료·디저트는 제외한다. 명백한 음식 형식과 근거 검증은 유지하고 의미 충돌은 모델 재심사와 식별자에 묶인 근거로 처리한다.
- 식당/지점/메뉴 정규화, 가격·배달 근거, 주재료 태그는 공통 경로에서 수정한다. 화면 한 곳만 임시 교정하지 않는다. 식사·평가 이력과 정책 시행 시각은 불필요한 메타데이터가 아니다.
- 사이트의 기존 용어·정보량·기능을 유지한다. 선호율, 다중 카테고리 선택, 수동 탐색, 접근성을 보존한다. 자동 화면 갱신은 추가하지 않는다.
- 날씨는 기상청·에어코리아만 사용한다. Open-Meteo는 사용하지 않는다. 날씨 문구·이모지·순서를 유지한다.
- 비밀값은 .env 및 프로젝트 밖 보호 상태에 둔다. 로그·문서·공개 스냅샷·Git에 넣지 않는다. OAuth 갱신 실패 중 생성된 더 최신 토큰도 보존한다.
- 운영 데이터 변경은 잠금·원자적 저장·동시 변경 검사를 사용한다. 로컬 비상 활성화는 소스·스냅샷·서버 장애·후보 준비량을 검증하고 제한된 임대로만 허용한다.
- `SERVER/_CLEANUP/_HANDOFF.md`는 이 작업 범위 밖이며 수정하지 않는다.
- 제품 버전은 2.6, package semver는 2.6.0이다. 2026-09-23 정식 출범 표기는 최종 검증 뒤 기록한다. 사용자 지정 릴리즈 표기는 GPT-6 Sol Max (Daybreak Blue)이며 실제 봇 수행 모델과 구별한다. 제공자의 내부 배포 번호는 제품 버전이 아니다.
- 검증 근거·잔여 조건을 QUALITY_REPORT.md와 HANDOFF.md에 기록한다. PASS는 명시된 검증 범위에 한정하며 모든 미래 장애의 부재를 보증하지 않는다. 최종 인계 시각은 KST로 기록한다.
