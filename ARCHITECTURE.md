# Ojeommwo v3 운영 구조

## 실행과 역할

pororo 호스트 cron이 `ojeommwo` 컨테이너의 봇을 호출한다. Socket Mode 리스너 하나가 상시 동작하며 내장 발송 스케줄러는 꺼져 있다. 후보 갱신은 평일 08:50·11:35·15:00·17:35, 캐시 발송은 11:25·17:25 KST다. 공휴일을 확인한다. 매일 07:40 실제 모델 인증 호출을 수행한다. 11:35에는 준비량이 충분해도 신규 식당을 선택적으로 조사한다.

`src/config.js`는 GPT-6 Luna xhigh와 읽기 전용 검색 환경을 검증한다. 모델은 웹 조사·입력 의미 정규화·분류 재심사를 수행하고 구조화 JSON을 반환한다. 정렬·선호·중복·TTL·파일 트랜잭션은 코드가 담당한다. 모델에 운영 DB 쓰기 권한을 주지 않는다.

## 후보와 최신성

기존 후보와 순환 카탈로그를 실제 지점 본문으로 재검증하고, 부족하면 모델로 보강한다. 어떤 유효한 세 메뉴가 먼저 선택되어도 다음 세 메뉴가 남는 두 끼 준비량을 검사한다. 선택적 신규 탐색은 검색 최대 6회, 목표 2개 식당, 단일 조사 최대 420초다. 분류 충돌은 별도 재심사한다. 선택 탐색의 실패는 이미 검증된 준비 풀을 실패 처리하지 않는다.

모델이 지점 좌표를 직접 확인하지 못하면 null로 반환한다. 서버가 허용된 정확한 지점 페이지의 구조화 좌표를 읽어 거리 조건을 확인해야 저장된다. 모델이 같은 위치를 지도로 반복 검색하거나 좌표를 추측할 이유가 없다. 지점 본문과 결합된 메뉴 가격·명시적 배달 신호·폐점 신호를 별도로 검사한다. 일치하는 현재 메뉴판의 명확한 새 가격은 갱신하고, 공급자 사이 가격 충돌은 보류한다. 방문·검증하지 않은 URL이나 URL 뒤 모델 설명은 근거로 저장하지 않는다.

기준은 전북대학교 공대 7호관 `35.8461205, 127.1340012`, 최대 6km이며 likely 배달 후보는 가까운 곳을 우선한다. 가격 TTL 7일, 배달 및 HTML 검증 TTL 3일이다. 다음 발송까지의 유효성을 검사한다. 폐점·지점 불일치는 무효화하고 일시적 공급자 장애와 구별한다. `likely`는 지점 배달 운영 근거이지 목표 주소의 현재 결제 가능 보증이 아니다. 매장가·배달 결제 가격도 구별한다.

## 정규화·분류·재료

`text.js`, `category-arbitration.js`, `verified-candidates.js`를 입력·검색·마이그레이션·출력에 공통 적용한다. 후토마끼/후토마키, 띄어쓰기, 더 담다의 지점 표기 등을 통합하되 실제 다른 지점·중량·단품/세트는 보존한다. 명백한 trash 입력은 거절하고, 그럴듯한 느슨한 식사 입력은 현재 전북대 주변의 실제 메뉴 근거로 정규화한다.

식사 카테고리 19개만 허용한다. 명백한 조리 형식과 검증된 제품 규칙을 보호하고, 약한 상호·단어 추정과 모델 의견의 충돌은 Luna가 실제 지점 메뉴판을 참고해 재심사한다. 높은 확신의 결정은 정규화된 식별자에 묶는다. 이름이 바뀌면 과거 결정을 재사용하지 않는다.

운영의 단백질 축과 관측소의 세부 검색 태그는 목적이 다르다. 운영은 겹치지 않는 주요 단백질로 추천 다양성을 확보하고, 사이트는 pork·mushroom·rice 같은 검색을 제공한다. 상호에 든 재료명은 메뉴 재료로 추정하지 않는다. 통새우와퍼는 공식 근거에 따라 beef+seafood다. 선택한 단백질이 기록되지 않은 레반트의 일반 케밥은 특정 고기를 단정하지 않는다.

## 데이터와 선호

7개 JSON 운영 저장소는 추천 이력·발송 영수증·실제 식사·후보/카탈로그·설문·커피 참여·전송 outbox다. 크기·스키마·참조 무결성, 잠금, 원자 rename, fsync, 복구 사본, 작업 식별자로 중복과 동시 변경을 통제한다. Slack 응답이 불확실한 발송은 outbox로 관리하며 무조건 재전송하지 않는다.

선호는 Beta(3,3), 설문 0.9, 실제 식사 1.0, 반감기 180일이다. 같은 사람·메뉴·KST 날짜의 설문은 최신 한 건, 서로 다른 최근 3일의 반응은 1/0.5/0.25배다. 실제 식사는 같은 날 또는 그 이전 설문보다 우선한다. **그 이후의 새로운 설문은 반영하며**, 더 오래된 실제 식사 영향은 새 설문 날짜 수에 따라 1→0.5→0.25→0으로 줄인다. 메뉴·식당·카테고리의 관련 관측은 제한된 가중치로 공유한다. 최종 정렬에는 쿨다운·다양성·탐색 18%가 함께 작용한다. 화면 선호율은 단순 득표율이 아니다.

## 사이트·비상 운영

Sites Worker/R2에는 정제된 공개 스냅샷만 게시한다. pororo가 10분마다 생성·검증·원자 게시하고 오프셋 health가 실제 공개 API 해시를 확인한다. 사이트는 접속할 때 최신 데이터를 읽으며 사용 중 자동 갱신하지 않는다. 알고리즘 소스 지문을 비교하므로 예전 프런트와 새 선호 계약이 조용히 섞이지 않는다.

Windows 비상 사본은 서버 소스 seal, 정적 출력, 7개 저장소 해시를 맞춘다. 활성화는 최대 24시간 수동 임대이며 서버 정상·복구·임대 만료·불일치 때 발송을 막는다. 자동 offsite 백업은 없으며 오래된 사본으로 임의 최신 데이터를 만들어내지 않는다.
