# v2.6 운영 설계

## 실행 경계

pororo 호스트 cron이 `ojeommwo` 컨테이너의 스크립트를 호출합니다. Slack Socket Mode 리스너 하나만 상시 동작하며 내장 발송 스케줄러는 꺼져 있습니다. 점심·저녁 발송은 캐시 모드입니다. 검색 실패가 곧 발송 실패가 되지 않도록 후보 생성과 발송을 분리합니다.

후보 갱신은 평일 08:50·11:35·15:00·17:35, 발송은 11:25·17:25입니다. 검색 전에는 기존 검증 후보와 순환 카탈로그를 재검증합니다. 준비량이 부족하면 Luna xhigh가 검색합니다. 11:35에는 준비량이 충분해도 한 번의 선택적 신규 식당 탐색을 수행합니다. 선택 탐색이 실패해도 검증된 준비 풀을 보존합니다. 다음 발송 시각까지 유효한 근거와 두 끼 예비 세트의 다양성을 검사합니다.

## 모델과 코드의 역할

`config.js`가 gpt-6-luna / xhigh / web-search를 고정 검증합니다. 모델은 후보 검색, 느슨한 입력의 실제 지점·메뉴 확인, 의미 분류 재심사를 맡습니다. Linux 읽기 전용 샌드박스와 독립 OAuth 환경에서 구조화된 JSON만 반환합니다. 모델은 DB에 직접 쓰지 않습니다.

분류는 음식 형식과 허용 목록을 보호하고, 약한 상호 추측이나 복수 형식의 충돌은 모델의 높은 확신 판정을 식별자에 묶어 저장합니다. 이름이 바뀌면 과거 판정을 재사용하지 않습니다. 권한 값은 `model-*`로 저장해 향후 모델 교체와 독립적이며, 예전 `luna-adjudicated` 값은 읽기 호환 후 마이그레이션 시 표준화합니다. 단일 정규식 또는 모델의 단독 판단에 전부 의존하지 않습니다.

`text.js`, `category-arbitration.js`, `verified-candidates.js`가 검색·입력·마이그레이션·출력에 공통으로 사용됩니다. 지점은 실제 후보 ID에 포함하며, 반복 추천을 막는 상호 쿨다운은 브랜드 단위입니다. 동일 메뉴의 띄어쓰기·표기 차이를 통합하지만 실제 다른 지점, 단품/세트, 중량은 무조건 합치지 않습니다.

## 데이터와 선호

추천 이력, 발송 영수증, 실제 식사, 검증 후보/카탈로그, 추천 설문, 커피 참여, 전송 outbox의 JSON 저장소를 사용합니다. 현재 규모에서는 별도 DB 서버가 필요하지 않습니다. 크기·스키마·참조 무결성을 검사하며 잠금, 원자적 rename, fsync, 복구 사본, 작업 식별자로 일관성과 재실행 안전성을 유지합니다. 불확실한 Slack 전송은 outbox에 남겨 무조건 재발송하지 않습니다.

선호는 Beta(3,3) 사전값, 식사 평가 가중치 1, 설문 가중치 0.9, 180일 반감기입니다. 같은 사람/식당/메뉴의 설문은 하루 최신 한 건을 최대 3일 반영하며 지난 2일의 영향은 각각 0.5·0.25배로 줄입니다. 실제 식사 경험이 있으면 그 사람의 해당 메뉴 설문보다 우선합니다. 메뉴·식당·카테고리의 관련 관측은 제한된 가중치로 공유됩니다. 최종 정렬에는 다양성, 쿨다운과 탐색 비율 18%도 적용됩니다. 이것은 만족 확률의 보장이나 단순 득표율이 아닙니다.

## 최신성

전북대 공대 7호관 기준 좌표 35.8461205, 127.1340012를 사용합니다. 후보는 지정된 전주 반경, 실제 지점 주소·좌표, 메뉴·가격, 배달 운영 근거를 검증합니다. 가격 TTL 7일, 배달 TTL 3일이며, HTTP 페이지 검증 자체도 더 짧은 기간을 따릅니다. 명시적인 폐점·배달 중단은 후보와 카탈로그에서 무효화합니다. 검색 결과의 존재나 사이트 HTTP 200만으로 배달 가능을 인정하지 않습니다.

배달 플랫폼의 주소별 주문 가능·그 시각 주문 마감·배달비·결제 가격은 공개 페이지로 확정할 수 없습니다. 표시된 매장가와 주문 앱의 가격이 다를 수 있습니다. 사이트의 과거 기록은 최근 확인 여부를 구분하고 새 추천은 유효한 근거를 다시 통과해야 합니다.

## 관측소와 비상 사본

Sites Worker와 R2는 공개용으로 정제한 스냅샷만 받습니다. 운영 DB·Slack 사용자 ID·API 키는 전송하지 않습니다. pororo가 10분마다 DB를 검증하고 해시가 일치하는 스냅샷을 게시하며 5분 간격 health로 확인합니다. 브라우저는 열 때 최신 스냅샷을 읽으며 사용 중 자동 새로고침하지 않습니다.

로컬 비상 사본은 소스 seal과 7개 운영 저장소 해시를 비교합니다. 활성화는 최대 24시간의 수동 임대이며, 서버 복구·임대 만료·불일치가 발견되면 중지합니다. 서버·로컬 병행 발송을 허용하지 않습니다. 자동 offsite 백업이 없으므로 장애 시점의 모든 최신 데이터 보존은 보장하지 않습니다.
