# v2.6 운영 모델 평가 — 2026-09-23

## 현행 계약

운영 `.env`와 `src/config.js`는 `gpt-6-luna` / `xhigh`를 강제한다. 서버 Codex CLI 0.154.0은 같은 ChatGPT 계정에서 이 모델을 거절했으나 안정판 0.156.1로 갱신한 뒤 구조화 호출이 성공했다. 별도 API 키는 사용하지 않는다. 모델은 웹 후보 조사, 모호한 식사 입력의 실제 상호·메뉴 확인, 음식 분류 충돌의 재심사에 쓰인다. 선호·정렬·중복·TTL·DB 쓰기는 코드가 수행한다.

운영 서비스 인증으로 실행한 구조화 인증 점검은 2026-09-23 20:37 KST에 성공했다. 입력 13,553, 출력 39(이 중 추론 출력 22), 총 13,592토큰, 약 9.6초였다. 분류 회귀 예시 10건의 구조화 응답은 10건 모두 프로젝트의 기대 카테고리와 일치했다. 이 예시는 분류 지침에 주요 경계가 포함된 연동 점검이며 새 식당·새 메뉴 전반의 독립 정확도 표본이 아니다.

## 적합성과 경계

Luna xhigh는 현재 세 가지 언어 판단 작업을 실제 CLI 경로에서 수행할 수 있다. 음식 형식이 명백한 경우에는 규칙과 검증된 식별자에 묶인 분류를 먼저 적용하고, 의미가 충돌할 때 모델이 독립 재심사한다. 모델의 자신감만으로 실제 메뉴판·가격·배달·폐점 근거를 통과시키지 않는다. 근거를 충분히 확보하지 못하면 후보를 보류하고 기존 안전한 후보를 보존한다. 이 설계가 모델 한 번의 오류를 바로 발송과 DB 오염으로 이어지지 않게 한다.

선택적 신규 식당 탐색은 평일 11:35에 준비 후보가 충분한 경우에만 한 번 수행한다. 신규 식당 한 곳을 목표로 검색 4회, 단일 모델 호출 최대 5분으로 제한한다. 실패하거나 검증 후보가 없더라도 준비된 두 끼 후보를 보존한다. 준비량이 부족하면 최대 5번의 보강 시도를 허용하므로 그날의 검색 토큰과 시간은 훨씬 커질 수 있다. 원격 검색 결과의 품질은 배달 플랫폼의 공개 정보량에 좌우된다.

## 토큰과 비용 해석

작은 구조화 호출도 Codex CLI의 도구·지침 문맥 때문에 실제 입력이 약 1.4만 토큰이었다. 분류 10건의 한 묶음은 입력 14,001, 출력 552(추론 출력 252 포함), 총 14,553토큰, 18.3초였다. 2026-09-23 실제 신규 식당 dry-run 한 번은 입력 732,600(캐시 입력 633,088 포함), 출력 9,114(추론 출력 8,019 포함), 총 741,714토큰·234초를 썼지만 엄격한 가격·배달 근거를 만족하는 신규 후보는 0건이었다. 이 값은 **성공적인 신규 추가의 보장치나 상한이 아니라** 검색·페이지 본문이 길어질 수 있음을 보여주는 단일 관측치다. 운영의 `codex-runs/*-telemetry.log`에서 작업별로 측정한다. 추론 출력은 출력 토큰에 이미 포함되어 다시 더하지 않는다.

[공식 GPT-6 Luna 모델 문서](https://developers.openai.com/api/docs/models/gpt-6-luna)의 참고 API 단가는 100만 토큰당 입력 $0.10, 캐시 입력 $0.01, 출력 $0.50이다. 위 실측 탐색의 기록된 토큰을 단순 텍스트 요율로만 계산하면 캐시 유지 시 약 $0.021, 전량 캐시 미스 시 약 $0.078이다. 이는 API 청구액 추정치가 아니다. 캐시 쓰기 요금, 검색 도구 비용, 개별 요청의 긴 문맥 할증은 포함하지 않았고 집계 텔레메트리만으로 실제 청구 단위를 재구성할 수 없다. 현재 서비스는 ChatGPT 계정의 Codex OAuth로 실행하므로 이 숫자는 별도 청구액이나 Pro 20x의 잔여 한도를 뜻하지 않는다. 사용 한도는 계정 전체·시점에 따라 달라져 이 봇에 보장된 토큰 수를 산출할 수 없다.

모델 또는 프롬프트의 정확한 접두부가 바뀐 첫 호출은 prompt-cache miss가 정상이다. 새 작업자나 새 모델은 과거 캐시가 있다고 가정하지 말고 `codex --version`, `node scripts/check-codex-auth.js`, 카테고리·정규화 회귀, 실제 웹 근거 통과율을 다시 확인한다. 계정 인증 만료·철회는 재로그인이 필요하며, 별도 API 과금 전환은 현재 운영 정책에 포함되지 않는다.
